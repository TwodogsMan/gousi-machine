# DESIGN.md · 狗屎机 // GX-01

> 设计规范文档。全局反 AI 味硬约束见 `~/.dsh/AGENTS.md`，本文件是**本项目视觉方案的唯一权威来源**。

---

## 1. Visual Theme & Atmosphere

**一句话定调**：一张绿毡赌桌。八个骰盅、一副牌、一个轮盘，庄家发一张构思卡给你。

**氛围关键词**：毡布 · 皮革护垫 · 卡纸 · 铜牌 · 骰子 · 扑克

**为什么是赌场**：这个工具的名字叫「构思机 / 狗屎机」（拼音 gousi ↔ goushi），做的事是**把命运交给随机数**——投骰子决定你的下一个项目。赌场是"随机数决定命运"这件事最直接的物质形态，而且里面的每样东西（骰子、牌、筹码、轮盘）都是现成的视觉词汇。

**视觉纪律（换风格不换纪律）**：全局反 AI 味黑名单**全部继续适用**——0 圆角、无美术渐变、无 `backdrop-filter`、无浮起阴影、无 emoji、颜色只走 token。

> ⚠️ 赌场风格最容易翻车的地方就是**金渐变 + 圆角筹码 + 发光描边**，那正是 AI 味的重灾区。所以本方案的做法是：只用**平涂 + 1px 线 + 材质纹理（重复渐变）**，靠"材料"而不是"光效"营造质感。

**旧风格去向**：v1.1.0 及以前是赛博朋克终端（`#00FF41` 荧光绿 + 品红 + 扫描线）。v1.2.0 整体换为赌桌材质，旧配色已由 `tools/check.mjs` 的「无旧风格配色残留」断言保证彻底清空。

## 2. Color Palette & Roles

所有颜色都是 CSS 变量，**零硬编码 hex**（`:root` 之外不出现 hex；canvas 配色常量化并在 §8 登记）。

### 2.1 毡布与桌面

| Token | 值 | 角色 |
|---|---|---|
| `--bg` | `#0A2A1C` | 毡布底：赌桌台面 |
| `--surface` | `#0E3423` | 毡布亮一档：行悬停 |
| `--surface-2` | `#123D29` | 再亮一档：按钮悬停 |
| `--surface-3` | `#082013` | 凹槽：轮盘坑 / 记分牌 / 发牌靴 |
| `--hole` | `#04120B` | 贯穿孔：桌洞 / 反白字底 |
| `--paper` | `#F6F3E8` | 卡纸象牙白：主文字 |
| `--card` | `#F2EEE1` | 卡面 |
| `--card-dim` | `#DED8C4` | 卡面阴影档 / 卡纸分隔线 |

### 2.2 语义色（变量名沿用，语义已改为赌桌 token）

| Token | 值 | 赌桌含义 | 功能含义 |
|---|---|---|---|
| `--green` | `#C9A227` | 黄铜 / 铜牌 | **主信息**：状态、命中、主 CTA |
| `--magenta` | `#C8102E` | 胭脂红 / 红桃 | **动作**：主要按钮、当前选中 |
| `--cyan` | `#E4C55C` | 淡金 | 次强调：区块标题 |
| `--amber` | `#C9A227` | 金 | 约束 |
| `--red` | `#C8102E` | 红 | 雷区 / 危险 |
| `--black` | `#14120F` | 黑桃 | 卡面上的黑墨 |
| `--ivory` | `#E9E4D5` | 象牙 | 中性前景 |

**为什么保留旧变量名**：`--green` 现在是黄铜色，看着别扭，但改名会牵动 300+ 处引用，收益只有"名字好看"。选择**改语义注释而不是改键名**，并在 §8 登记 canvas 镜像。这是有意的取舍，不是遗漏。

### 2.3 卡面（浅底）专用

卡纸是浅色底，需要一套**独立于毡布**的墨色层级，否则毡布上的 `--ink` 放到纸上就看不见了。

| Token | 值 | 角色 |
|---|---|---|
| `--ink-on-card` / `2` / `3` / `4` | `#25231C` / `#3C3A2E` / `#6B6752` / `#8C8871` | 纸上正文 / 次级 / 标签 / 符号 |
| `--tag-on-card` | `#4A4738` | 纸上 badge 文字 |
| `--ink-brass` | `#8A7620` | 纸上黄铜（约束块） |
| `--ink-green` | `#2F6B3E` | 纸上绿（成立判据块） |
| `--gold-lite` / `--gold-pale` / `--gold-mid` / `--gold-deep` | `#E8D48A` / `#F0E3B4` / `#B9A24E` / `#7A6A1E` | badge 与强调的四档金 |
| `--warn-on-card` / `--ok-on-card` | `#F2E2E0` / `#E4EDDF` | 雷区块 / 成立判据块的纸面染色 |
| `--name-edge` / `--name-on-card` | `#9C8F63` / `#EDE6CE` | 名字候选的虚线框与底 |

### 2.4 扑克

| Token | 值 | 角色 |
|---|---|---|
| `--rail-red` | `#7A1414` | 桌沿皮革红：粗边框、双线、选中反白底 |
| `--rail-blk` | `#0F0C08` | 桌沿皮革黑：外圈护垫 |

**配色纪律**：
- **红是"命运色"**：桌沿（结构）+ 动作（交互）+ 雷区（语义），三处共用一色是刻意的——赌场里红就是桌子和结果的颜色。
- **金只做强调，不做背景**：一屏内金黄色块不超过 3 处，否则变成"土豪金"，那是 AI 味的另一种形态。
- **禁止**：金渐变、圆角筹码、发光描边、`backdrop-filter` 上的金属反光。

## 3. Typography Rules

```
--mono: "Cascadia Mono","Consolas","JetBrains Mono","DejaVu Sans Mono","Noto Sans Mono CJK SC","Microsoft YaHei Mono",monospace
--cjk:  "SimSun","宋体","Noto Sans Mono CJK SC","Microsoft YaHei",sans-serif
```

**全部使用系统字体栈，零网络请求**——这是本工具的硬约束（离线可用），不接受 Google Fonts 依赖。

| 用途 | 字号 | 字重 | 字距 | 字体 |
|---|---|---|---|---|
| 主标题 H1 | 30px | 700 | -0.5px | mono（RGB 错位阴影） |
| 构思卡标题 | 25px | 700 | -0.5px | mono（RGB 错位阴影） |
| 区块标题 | 10.5px | 400 | 2px | mono，大写 |
| 卡片数值 | 14.5px | 400 | 0 | mono |
| 命题 / 长文 | 16px | 400 | 0.02em | cjk，行高 1.9 |
| 正文说明 | 12.5–14.5px | 400 | 0.02em | cjk，行高 1.8 |
| 元信息 / 标签 | 10–11.5px | 400 | 1–2px | mono |

**中文排版要求**（来自 style-seed「中文优雅」，本方案沿用）：
- 中文正文行高 ≥ 1.7，本方案取 1.8–1.95（终端风需要更松的呼吸）
- 中文用 `letter-spacing: 0.02em`，英文标签用 1–2px 拉开
- 中文正文 ≥ 15px，长文阅读 ≥ 16px
- 中英混排时中文字族在前（`--cjk`），等宽字族作为数据与标签的主字族

**禁止字体**：Inter、Roboto、Arial、Space Grotesk、以及任何几何无衬线"科技感"字体（这些是 AI 默认脸）。

## 4. Component Stylings

**统一规则：`--r: 0px`，全项目禁止任何圆角**（唯一例外：转盘中心圆钮与 canvas 圆形，属几何必需）。

### 按钮 `.c`
- default：`--surface-3` 底 + `1px solid var(--line-2)` + `--ink` 文字（像嵌在桌面上的铜牌）
- hover：边框与文字转 `--green`（黄铜）
- active：`transform: translate(1px,1px)`（物理下沉 1px，不用缩放）
- disabled：文字与边框降到 `--ink-lo` / `--line`，`cursor: not-allowed`
- primary：**黄铜实底 + 深墨字**（不是发光描边）；hover 转卡纸白
- focus-visible：`2px solid var(--green)` + `outline-offset: 2px`（全局）

### 老虎机行 = 八张牌 `.slot`
- 三列网格：`30px` 骰面 | 自适应内容 | `38px` 工具列
- **骰面是真点阵**：3×3 的 9 个 `<i>`，靠 `data-face="1..6"` 决定显示哪几个点（1 中心 / 2 对角 / 3 对角+中心 / 4 四角 / 5 四角+中心 / 6 六面无中心）
- 八行固定用 `(i%6)+1` 分配骰面，保证 1–6 全覆盖
- 工具列用**花色**而非字母：`♠` 洗这张牌（单格重抽）、`♦` 压注保留（锁定）
- 行间仅 1px 分隔线，**不使用卡片容器、不用阴影、不用圆角**
- 锁定态：`--tint-green` 暗金底 + 骰点转黄铜 + `♦` 反白为胭脂红底
- 滚动中：数值转黄铜 + 骰点转卡纸白，说明文字降到 25% 透明
- 命中：`steps(2,end)` 阶跃闪烁一次（不做平滑淡入）

### 构思卡 = 一张牌面 `.brief`
- **整卡是浅色卡纸**（`--card` 底 + `--black` 墨），与毡布形成材质反转——这是全场唯一的浅色区域，视觉焦点自然落在这里
- 四角 `♠` 与右上角 `♦ DEALER ♦` 水印，构成扑克牌的角标语言
- 标签 `.badge`：`margin: 0 -1px -1px 0` 形成**紧凑拼接网格**，四档金 + 红
- 正文区：`auto-fit minmax(290px,1fr)` 自适应网格，区块之间用 1px **卡纸分隔线**（`--card-dim`）
- 约束区块 `.rule`：`!` + 纸上黄铜；雷区 `.no`：`×` + 红 + 淡红纸底；成立判据 `.ok`：`✓` + 纸上绿 + 淡绿纸底
- 开工步骤：`01`–`05` 反白编号（黑底卡纸字）
- 名字候选：黄铜虚线框 + 米黄底

### 轮盘 `.hub` / `.chips` / `.item`
- canvas 扇区 **红黑交替**（`i%2 ? WHEEL_RED : WHEEL_BLACK`）+ 象牙分隔线 = 轮盘格
- 黄铜外圈 + 72 格刻度（每 6 格一条象牙长刻）+ 每扇区一颗黄铜珠 = 轮盘小球停靠位
- 中心钮为唯一允许的圆形，`106px`，**双线红边**（`3px double var(--rail-red)`），像轮盘的轴帽
- 分组切换 = 无缝紧贴的 tab，激活态黄铜底深墨字
- 元素行命中：`inset 3px 0 0 var(--rail-red)` 左侧红标

### 输入
- 单行：透明底 + 边框，focus 时底色 `--tint-green`
- 多行：`1px solid var(--line-2)`，focus 转黄铜
- 无圆角、无阴影、无浮动标签

## 5. Layout Principles

- 容器 `max-width: 1240px`，左右 `22px`，**非居中英雄式布局**
- 主栅格：`340px | 1fr`，外面套 **10px 皮革护垫**（`--rail-blk` 实底 + `::after` 的 `--rail-red` 2px 线 + 90° 压纹），形成"一张赌桌"
- 全屏 `body::after` 再用 `inset box-shadow` 画一圈红黑桌沿——**用 inset 而不是普通阴影**，因为它是"桌子的边框"不是"卡片的浮起"
- ≤900px：单列堆叠，护垫降到 6px
- ≤600px：桌沿降到 6px + 8px
- 间距梯度：4 / 7 / 11 / 14 / 18 / 22 px（紧凑，记分牌密度）
- **不使用卡片网格铺满页面**，页面骨架是"面板 + 分隔线"

## 6. Depth & Elevation

**没有阴影体系**。层次靠三样东西建立：
1. 边框明度差（`--line` → `--line-2` → `--dim`）
2. 底色差（`--bg` → `--surface` → `--surface-2`，以及暗绿/暗黄/暗红的功能底色）
3. 霓虹发光（**仅用于文字**，`text-shadow: 0 0 Npx`，且只给关键状态：主标题、卡片标题、命中值、primary 按钮）

`box-shadow` 全项目只出现两次，都是描边式（`0 0 0 1px`）或绿色泛光，不用来模拟"卡片浮起"。

## 7. Animation & Interaction

**动效档位：L2–L3**，但强制性能红线：

| 动效 | 实现 | 说明 |
|---|---|---|
| 招牌双层投影 | 静态 `text-shadow`（红 + 黑） | 零成本，像霓虹招牌的背面阴影 |
| 发牌抖动 | `steps(3,end)` 关键帧位移 + 透明度 ×2 | 一次性，0.7s 内结束 |
| 数值读取 | `requestAnimationFrame` 字符替换（`▓▒░#@%&$*`） | 180ms，只在该格重抽时 |
| 骰盅摇动 | `setInterval` 音效 + 骰点转卡纸白 | 420ms |
| 轮盘 | `easeOutQuart` 5–7 圈，3.3s，逐帧重绘 canvas | 单次，结束即停止 rAF |
| 花色闪烁 | `steps(1)` 1.1s 无限 | `♦`/`◆` 呼吸，可 `prefers-reduced-motion` 关闭 |
| 毡布织纹 / 桌沿 | 固定定位伪元素，重复渐变 + `inset` 描边 | 静态层，不参与滚动重绘 |

**性能红线（不可破）**：
- 移动元素上**禁止** `filter: blur()`；全项目 `backdrop-filter` 使用量为 **0**
- **禁止**在滚动区域使用大面积 fixed 重绘层；毡布织纹层为静态
- 转盘结束后必须停止 `requestAnimationFrame`（已满足）
- `prefers-reduced-motion: reduce` 时：全部动画与过渡归零、招牌投影移除

## 8. Do's and Don'ts

**Do**
1. 用 1px 边框、底色明度差、**材质纹理**建立层次，不用阴影
2. 把每个视觉都解释成赌桌上的**实物**（毡布/护垫/卡纸/铜牌/骰子/扑克/轮盘），解释不出来就别加
3. 红是命运色（桌沿 + 动作 + 雷区），金只做强调——一屏金黄色块不超过 3 处
4. 浅底区域（卡纸）必须用独立的墨色 token（`--ink-on-card*`），不能复用毡布上的 `--ink`
5. 中文正文行高 1.8+、字距 0.02em、字号 ≥15px
6. 所有可交互元素必须有 hover + focus-visible 态
7. canvas 配色必须由 `WHEEL_*` 常量供给，且每个常量对应一个 `:root` token
8. 保持零网络请求：系统字体栈 + 无外部资源

**Don'ts**
1. ❌ 任何非 0 圆角（除轮盘中心钮与 canvas 圆形这类几何必需）
2. ❌ 任何美术渐变填充——渐变只允许 `repeating-` 质感纹（毡布织纹、皮革压纹）与 1px 细线
3. ❌ **金渐变 / 圆角筹码 / 发光描边**——赌场风格的 AI 味重灾区，一律禁止
4. ❌ `backdrop-filter` 毛玻璃、金属反光、半透明玻璃卡片
5. ❌ 紫色 / 蓝紫渐变
6. ❌ 居中一切 + 统一圆角卡片网格
7. ❌ emoji 当图标（本方案用花色与制表符：`♠ ♦ ◆ × ✓ ▶ ↻ ↓`）
8. ❌ Inter / Roboto / Arial / 系统默认无衬线当主字族
9. ❌ 用普通 `box-shadow` 模拟卡片浮起（只允许 `inset` 桌沿描边与 `0 0 0 1px`）
10. ❌ 让动效影响布局（避免 `width/height/top/left` 参与动画）

## 9. Responsive Behavior

| 断点 | 行为 |
|---|---|
| >900px | 双列 `340px \| 1fr`，外框 10px 皮革护垫；构思卡内部 `auto-fit minmax(290px,1fr)` 多列 |
| ≤900px | 主栅格单列堆叠；护垫降到 6px；构思卡内部自动降为单列 |
| ≤600px | 桌沿降到 6px + 8px；命令按钮自动换行；轮盘 `min(400px,80vw)` 自适应 |
| ≤320px | 标签 `.badge` 紧凑拼接仍不溢出；卡片正文 `word-break: break-word` |

**触摸目标**：命令按钮 padding 12px 16px（≥44×44px）；行内工具按钮 38×38px；元素行工具 32px 宽（配合行高 ≥40px）。移动端无 hover 依赖，所有 hover 态都有等价的激活/选中态。

## 10. 质量自检结果

三支验证工具，全部必须通过。**复验命令**：

```bash
node tools/check.mjs         # 反 AI 味 + 逻辑回归（17 项）
node tools/check-style.mjs   # 赌场风格接线（16 项）
node tools/check-spec.mjs    # 导出双形态
```

```
狗屎机 // 验收清单实测
==========================================================
  [PASS] 圆角仅 0 / 圆形几何             共 1 处全部合规
  [PASS] 无美术渐变                    3 处质感渐变已豁免
  [PASS] backdrop-filter 为 0      0 处
  [PASS] 无 emoji 图标               0 个
  [PASS] 无浮起式阴影                   4 处均为描边/霓虹
  [PASS] CSS 无 :root 外的硬编码色       0 处
  [PASS] canvas 配色由 token 供给      6 个 canvas 常量全部对应 :root token
  [PASS] 无旧风格配色残留                 赛博朋克色系已清空
  [PASS] 有 focus-visible 态        已定义
  [PASS] 有 reduced-motion 降级      已定义
  [PASS] 零外链资源                    0 处
  [PASS] 含移动端断点                   @media (max-width:600px) / (max-width:900px) / (prefers-reduced-motion)
  [PASS] 骨架以 1px 分隔线为主            29 处 1px 分隔线
  [PASS] 逻辑回归 3000 张卡             0 异常, 唯一一句话 3000 种
  [PASS] 锁定语义正确                   锁定格保持不变
  [PASS] 工具名一致（防漂移）               「狗屎机」在 HTML + 文档中一致, 无旧名残留
  [PASS] localStorage key 稳定      仍为 gousi-machine-v1（改名不丢用户数据）
==========================================================
结果: 17/17 全部通过
```

`tools/check-style.mjs`（赌场风格接线，16 项）另测材料层与渲染结果：

```
  [PASS] 毡布织纹：两层错位重复纹          [PASS] 桌沿：红黑双线皮革护垫（inset 描边）
  [PASS] 桌框：皮革压纹包边                [PASS] 构思卡：卡纸底 + 黑墨字（浅底反转）
  [PASS] 骰子点阵规则：23 条 data-face     [PASS] 骰子 9 点全部定位（3×3 网格）
  [PASS] 花色语义：♠ 洗牌 · ♦ 压注 · ◆ 分区 · × 雷区 · ✓ 成立
  [PASS] renderSlots 输出 8 个骰子         [PASS] 共 72 个骰点（8×9）
  [PASS] 骰面分布：1,2,3,4,5,6,1,2（1-6 全覆盖）
  [PASS] 旧 R/L 字母按钮已换成花色 ♠/♦      [PASS] 轮盘配色常量化：6 个 WHEEL_*
  [PASS] 轮盘红黑格：i%2 交替填充           [PASS] 轮盘黄铜外圈
  [PASS] 圆角仍为 0                        [PASS] 无美术渐变
结果: 全部通过（材料层 / 骰子 / 花色 / 轮盘 / 底线）
```

- [x] 9+1 个区块都有实质内容，非模板占位
- [x] 所有颜色经 CSS 变量引用，`:root` 之外零硬编码 hex；canvas 配色由 token 常量化
- [x] 每个可交互元素含 default / hover / active / focus-visible / disabled 态
- [x] 动效档位明确（L2–L3），含 `prefers-reduced-motion` 完整降级
- [x] 响应式覆盖 Desktop / Tablet / Mobile / 320px
- [x] 零网络请求、零第三方依赖、零 emoji 图标、零美术渐变、零非零圆角
- [ ] **视觉渲染未经我本人目视确认**（headless 截图受沙箱限制）。以上全部是代码级自动断言——毡布纹理的粗细、卡纸与毡布的反差、轮盘红黑格的观感这类主观项需要你亲眼看一下

---

## 附：改动记录

### v1.2.0 — 赛博朋克终端 → 赌桌

| 项目 | 改前（v1.1.0 赛博朋克） | 改后（v1.2.0 赌桌） |
|---|---|---|
| 底材 | 纯黑 `#0A0A0A` | 毡布绿 `#0A2A1C` + 两层错位织纹 |
| 主色 | 荧光绿 `#00FF41` | 黄铜 `#C9A227` |
| 强调 | 品红 `#FF0080` + 青 `#00FFFF` | 胭脂红 `#C8102E` + 淡金 `#E4C55C` |
| 框架 | 1px 线 + CRT 扫描线 | 10px 皮革护垫 + 红黑双线桌沿 + 压纹 |
| 卡片 | 深色玻璃面板 | 象牙卡纸（浅底反转）+ 扑克角标 |
| 图标 | 终端制表符 | 骰子点阵 + 扑克花色 `♠ ♦ ◆` |
| 转盘 | 霓虹描边弧 + 刻度点 | 轮盘红黑格 + 黄铜外圈 + 黄铜珠 |
| 标题 | 品红/青 RGB 错位 | 红黑双层投影（招牌） |
| 动效 | 故障抖动 | 发牌抖动 |
| canvas | `#00FF41`/`#FF0080`/… 硬编码数组 | `WHEEL_*` 常量，逐个对应 `:root` token |

### v1.0.0 前史 — AI 默认脸 → 赛博朋克（已废弃，保留记录）

| 项目 | 改前（AI 默认脸） | 改后 |
|---|---|---|
| 圆角 | 10/13/14/20px | 全 0px |
| 配色 | 紫 `#7c5cff` → 青 `#22d3ee` 渐变 | 终端绿 + 品红 |
| 背景 | 三处 radial-gradient 光晕 | 纯黑 + 1px 网格 + 扫描线 |
| 层次 | `box-shadow` 卡片浮起 + 玻璃拟态 | 1px 边框 + 底色差 + 文字霓虹 |
| 骨架 | 居中容器 + 圆角卡片网格 | 面板双列 + 分隔线 |
| 图标 | emoji（🎲🎰🎡📒🔒） | 终端字形 |

---

## 附：项目专属须知

> 项目级 `AGENTS.md` 已删除（用户在 2025 年要求去掉，理由：全局 `~/.dsh/AGENTS.md` 已覆盖通用规则，项目文件冗余）。**本文档现在是本项目界面规范与规则的唯一权威来源。**

### 规则来源

| 层级 | 位置 | 内容 |
|---|---|---|
| 全局 | `~/.dsh/AGENTS.md` | 11 条反 AI 味硬黑名单、工程质量底线、本机环境事实、沟通偏好。**对本机所有项目生效** |
| 项目 | 本文档 + `README.md` | 设计 token、组件规范、验收清单、踩坑记录 |

### 设计 token 全集（`index.html` 的 `:root` 是实现，这里是语义索引）

```
毡布/桌面  --bg #0A2A1C  --surface #0E3423  --surface-2 #123D29  --surface-3 #082013  --hole #04120B
卡纸      --paper #F6F3E8  --card #F2EEE1  --card-dim #DED8C4
线/文字    --line #1B4A32  --line-2 #266B4A  --dim #4E8A6B
          --ink #E9E4D5  --ink-dim #A9C4B3  --ink-lo #6E9A83
语义      --green #C9A227(黄铜/信息)  --magenta #C8102E(胭脂红/动作)  --cyan #E4C55C(淡金)
          --amber #C9A227(约束)  --red #C8102E(雷区)  --black #14120F(黑桃)  --ivory #E9E4D5
扑克      --rail-red #7A1414(桌沿皮革红)  --rail-blk #0F0C08(桌沿皮革黑)
暗底染色  --tint-green #1A2A0E   --tint-amber #241E08   --tint-magenta #2A0A12
描边      --edge-green #4A3D12   --edge-magenta #5C1220  --edge-cyan #4A3D12  --edge-amber #4A3D12
卡面墨色  --ink-on-card #25231C / 2 #3C3A2E / 3 #6B6752 / 4 #8C8871
          --tag-on-card #4A4738  --ink-brass #8A7620  --ink-green #2F6B3E
          --gold-lite #E8D48A  --gold-pale #F0E3B4  --gold-mid #B9A24E  --gold-deep #7A6A1E
          --warn-on-card #F2E2E0  --ok-on-card #E4EDDF  --name-edge #9C8F63  --name-on-card #EDE6CE
其它      --r 0px（禁止修改）  --mono（记分牌等宽）  --cjk（中文宋体栈）
```

配色纪律：**红是命运色**（桌沿 + 动作 + 雷区，三处共用是刻意的）；**金只做强调**，一屏金黄色块 ≤3 处。

### 已装设计 skill（在 `.claude/skills/`）

- `web-design/` — 10 套 style-seeds（**v1.2.0 的赌桌方案不再直接对应某个 seed**，是从"随机数决定命运"这个主题推导出来的材料语言）、58 个品牌设计系统、交互档位 L1–L3、质量清单
- `frontend-design/` — anti-slop 原则与 Tone Menu
- `theme-factory/` — 10 套预设主题 token
- `canvas-design/` — 海报/PDF 生成（字体二进制未下载，需用时再补）

⚠️ `bergside/awesome-design-skills` 的 67 套是模板套壳，正文全是同一段空话，没有组件规范，**别用**。

### 复验

```bash
node tools/check.mjs        # 反 AI 味 + 逻辑回归（17 项）
node tools/check-style.mjs  # 赌场风格接线（16 项）
node tools/check-spec.mjs   # 导出双形态（加 --show 可打印样例输出）
```

不许口头声称通过，必须贴实测输出。

### 导出双形态（v1.1.0 新增）

同一份构思卡导出两份，服务不同读者。**这不是换个标题，两种形态的结构完全不同**：

| | 人看版 `briefToMd()` | agent 执行版 `briefToAgentMd()` |
|---|---|---|
| 读者 | 人 | AI agent |
| 文体 | 叙事式，讲「为什么这么做」 | 规范式，祈使句，讲「做什么、做到什么算完」 |
| 结构 | 标题 + 10 个语义章节 + 名字候选 | YAML frontmatter + 8 个编号章节 |
| 机器可解析 | 否 | **是**（frontmatter 可被程序直接读取） |
| 文件名 | `构思-<主题>.md` | `SPEC-<主题>.md` |
| 章节目录 | 核心动作 / 强制约束 / MVP / 扩展方向 / 成立判据 / 雷区 / 开工顺序 | 0 任务 · 1 执行规则 · 2 硬约束 · 3 交付步骤 · 4 核心动作 · 5 验收标准 · 6 明令禁止 · 7 不确定时怎么办 |

agent 版里几处是**专门为「能被可靠执行」设计的**，不是装饰：

1. **YAML frontmatter** — agent 不用正则去啃正文，直接读结构化字段（`hard_constraints` / `deliverable` / `must_not` / `out_of_scope`）
2. **祈使句 + 编号** — 是命令不是描述，可直接当待办清单执行
3. **验收标准写成可核对形式** — 每条都是 `- [ ]`，且要求「必须实际跑一遍，不要只读代码就勾选」
4. **明确 OUT OF SCOPE** — 列出账号体系/后端/多端同步等，防止 agent 自作主张扩大规模
5. **明确不许提问 + 冲突裁决顺序** — 避免执行中途停下来等人，或方向跑偏
6. **一次只做第一步** — 做完停下等确认，防止一口气做完五步然后全部返工

两份**从同一份 brief 数据派生**（`plain()` 去标签），保证人看版和执行版语义不漂移——这是 `tools/check-spec.mjs` 会断言的。

### 历史踩坑（不要重犯）

1. **名字候选用 `Set` 去重 3 次抽样** → 14% 概率只剩 2 个候选。已改为真正的无重复抽样（`sample()`）。
2. **反 AI 味红线判定过宽** → 把 1px 网格细线和 `repeating-` 扫描线误判成美术渐变。正确判定：**非 `repeating-` + 至少两个不同色点**才算违规。
3. **CSS 里硬编码颜色** → 违反「颜色只能走 token」。已把 10 处（`#001a08`、`#0d3a1a` 等暗底染色）收进 `--tint-*` / `--edge-*`。
4. **`.claude/skills/` 层级套错** → `anthropics/skills` 原始路径是 `skills/<name>/`，直接下载会多套一层，必须拍平到 `.claude/skills/<name>/`，否则 agent 读不到。
5. **桌面端 `git clone` 下不了** → 本机 git 远端操作要 spawn `sh.exe`/`ssh.exe`，沙箱禁命名管道（`Win32 error 5`）。下载第三方文件用 Node 原生 `fetch` + GitHub API/raw。
6. **pitch 模板拼接出「把把」叠字** → `themeNote` 本身以「把」开头，模板又加了一个「把」，长期没被发现（断言只查 undefined，不查中文语法）。已改为 `把「${themeNote}」这件事`，并在 `check-spec.mjs` 里加了叠字断言（把把/的的/了了/是是/在在）。
7. **PowerShell `Set-Content` 把 UTF-8 文件写成 ANSI** → 中文字符全部损坏，文件变成 `invalid UTF-8` 无法读取。**改代码文件一律用 write/edit 工具，不要用 PowerShell 的 `Set-Content` / `Out-File` 做字符串替换。**
8. **换风格时用 edit 替换整个 `<style>` 块，把文件切坏过一次** → 我以为 `old_string` 会覆盖到 `</style>`，实际只匹配了开头一小段，结果新旧两套 CSS 并存、还在中途留下一个非法的提前闭合 `</style>`，后面 347 行旧 CSS 变成 raw text。补删时又因为两个标记都是 `</style>` 而删错了那一个，导致 `<style>` 永不闭合。**教训：大段替换前先把块的首尾行都读出来核对；替换后立即断言 `<style>`/`</style>`/`<script>`/`</script>` 数量各为 1。**
9. **改配色后忘了同步检查脚本** → `check.mjs` 原来断言 `WHEEL_COLORS` 含 `#00FF41` 等旧色，换赌桌配色后必然误报。约束应随方案演进：已改为「canvas 常量必须逐个对应 `:root` token」+「旧风格配色必须零残留」，比原来更严格。
